[pytest]
testpaths = tests
python_files = test_*.py
markers =
    ray: tests that spin up a real Ray/RLlib process (slow, excluded by default -- see README's "Running the Ray-backed tests" note)
addopts = -m "not ray"
